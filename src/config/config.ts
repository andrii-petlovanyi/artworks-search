const config = {
  BASE_API_URL: "https://api.artic.edu/api/v1/artworks/search",
  ITEM_FIELDS:
    "api_model, id, title, description, term_titles, thumbnail, image_id, api_link",
};

export default config;
